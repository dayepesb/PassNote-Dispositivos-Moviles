package co.edu.poli.passnote.passnote;

import org.junit.Assert;
import org.junit.Test;

import co.edu.poli.passnote.passnote.utils.EncryptionUtils;

public class ExampleUnitTest {
    public static final String TEST_STRING_TO_ENCRYPT = "Test string to encrypt";

    @Test
    public void encrypt() throws Exception {
//        String encryptedInfo = EncryptionUtils.encrypt(TEST_STRING_TO_ENCRYPT);
//        String decryptedInfo = EncryptionUtils.decrypt(encryptedInfo);
//        Assert.assertNotEquals(encryptedInfo, decryptedInfo);
//        Assert.assertEquals(TEST_STRING_TO_ENCRYPT, decryptedInfo);
    }
}